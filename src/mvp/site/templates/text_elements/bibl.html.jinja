{% from "components/macros.html.jinja" import render_children, reference_dropdown with context %}
{% if text_container.ref %}
    {{ reference_dropdown(text_container, text_container.ref, "tei-bibl") }}
{% else %}
    <span class="tei-bibl">{{- render_children(text_container) -}}</span>
{% endif %}
