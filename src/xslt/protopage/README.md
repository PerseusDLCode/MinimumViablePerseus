These stylesheets parallel the (older) HTML stylesheets and will eventually replace them.  They process TEI documents into protopage documents: chunks of XML extracted according to the TEI document's citestructure and converted from TEI XML to a simplified schema that is suitable for uniform HTML rendering.

